monitor alarm {

    condition c;
    int currenttick = 0;
    void  delay(int  ticks) {
        int argtick = ticks + currenttick;
        while (currenttick < ticks){
            
        }
    }
    void  tick() {
            // TODO: implement  tick
    }
}